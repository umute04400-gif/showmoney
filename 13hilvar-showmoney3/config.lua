Config = {}

-- Güncelleme ayarları
Config.UpdateInterval = 1000 -- Para kontrol aralığı (milisaniye)

-- Pozisyon ayarları
Config.Position = {
    top = "20px",
    right = "20px"
}

-- Renk ayarları
Config.Colors = {
    money = "#4ADE80" -- Para rengi (yeşil)
}

-- Font ayarları
Config.Font = {
    family = "Gala Condensed",
    fallback = "Arial, sans-serif",
    size = "18px",
    weight = "500"
}

-- Gösterim ayarları
Config.ShowDuringDeath = false -- Ölüyken para gösterimi
Config.Debug = false -- Debug mesajları